@model IEnumerable<LMS_Project.ViewModel.CourseListViewModel>

@{
    ViewData["Title"] = "Courses";
}

<!-- Bootstrap CSS for styling -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

<section class="py-5 bg-light">
    <div class="container">
        <h2 class="mb-5 text-center text-primary">Available Courses</h2>
        @if (TempData["Massage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show fixed top-4 right-4 z-50" role="alert">
                <strong>Success!</strong> @TempData["Massage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            
                TempData["Massage"] = null; // Clear TempData after displaying
            
        }
        @if (Model != null && Model.Any())
        {
            <div class="row">
                @foreach (var course in Model)
                {
                    <div class="col-md-4 mb-4">
                        <div class="card shadow-sm h-100 transition-transform duration-300 hover:shadow-lg hover:-translate-y-1">
                            <img src="@Url.Content(string.IsNullOrEmpty(course.ImageUrl)
                               ? "~/images/courses/default-course.jpg"
    :                            "~/images/courses/" + course.ImageUrl)"
                                 class="card-img-top"
                                 alt="@course.CourseName"
                                 style="height: 200px; object-fit: cover;" />

                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title text-primary">@course.CourseName</h5>
                                <p class="card-text"><small class="text-muted">Instructor: @course.InstructorName</small></p>
                                <p class="card-text"><small class="text-muted">Price: $@course.Price</small></p>
                                <a asp-controller="Student" asp-action="Details" asp-route-id="@course.Id"
                                   class="btn btn-outline-primary">View Details</a>
                                @if (User.IsInRole("Admin"))
                                {
                                    <a asp-action="DeleteCourse" asp-controller="Course" asp-route-id="@course.Id"
                                       class="btn btn-danger btn-sm mt-2">Delete Course</a>
                                    <a asp-action="EditCourse" asp-controller="Course" asp-route-id="@course.Id"
                                       class="btn btn-primary btn-sm mt-2">Edit Course</a>  
                                }

                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="alert alert-info text-center">
                No courses available at the moment.
            </div>
        }
    </div>
</section>

<!-- Bootstrap JS (optional, for interactivity) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

<style>
    .transition-transform {
        transition: transform 0.3s ease-in-out;
    }

    .card:hover {
        transform: translateY(-5px);
    }
</style>